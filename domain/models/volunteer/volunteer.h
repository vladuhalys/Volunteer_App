#include "../person/person.h"

class Volunteer
{
public:
// Fields

// Constructors
    Volunteer();
    Volunteer(const Volunteer& other);
    Volunteer(Volunteer&& other) noexcept;
    ~Volunteer();

private:

};